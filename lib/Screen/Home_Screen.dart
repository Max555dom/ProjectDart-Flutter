import "package:flutter/material.dart";
import 'package:formation/Pages/Home_Page.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: HomePage(),
    );
  }
}
